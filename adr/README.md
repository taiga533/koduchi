# ADR — アーキテクチャ決定記録

小槌（koduchi）の設計判断を記録する。各 ADR は「後から 1 件だけ覆す」ための単位で切ってある。

| #                                                    | 決定                                                                  |
| ---------------------------------------------------- | --------------------------------------------------------------------- |
| [0001](0001-oracle接続方式.md)                       | Oracle 接続方式 — `oracle` crate と Instant Client の別途インストール |
| [0002](0002-接続ごとのアクタースレッドと中止.md)     | 接続ごとのアクタースレッドと実行の中止、Driver trait の境界           |
| [0003](0003-結果セットのカーソル保持と接続プール.md) | 結果セットのカーソル保持と接続プール                                  |
| [0004](0004-認証情報と接続設定の保存先.md)           | 認証情報と接続設定の保存先、読み取り専用モード                        |
| [0005](0005-クエリ履歴とセッション復元のsqlite.md)   | クエリ履歴とセッション復元に SQLite を使う                            |
| [0006](0006-tnsnames解決を自前パースで行う.md)       | tnsnames.ora の解決を自前パースで行う                                 |
| [0007](0007-スキーマ取得の段階的ロード.md)           | スキーマ取得の段階的ロードとフィルタ                                  |
| [0008](0008-デザイントークンとテーマ実装方式.md)     | デザイントークンとテーマの実装方式                                    |
| [0009](0009-ウィンドウモデルとタイトルバー.md)       | ウィンドウモデルとタイトルバー、結果ペインの動的タブ                  |
| [0010](0010-テストとリンタの構成.md)                 | テストとリンタの構成                                                  |
| [0011](0011-cicdとリリース配布.md)                   | CI/CD とリリース配布 — 未署名の dmg を下書きリリースへ                |

## ADR に切り出していない決定

単独で覆す対象になりにくい、あるいは他の ADR に従属する決定をここにまとめる。

### 機能スコープ

|        | 内容                                                                                                      |
| ------ | --------------------------------------------------------------------------------------------------------- |
| 作る   | SQL 実行 / シンタックスハイライト / 結果テーブル / 実行計画 / クエリ履歴 / スキーマツリー / CSV 保存      |
| 器だけ | 保存済みクエリ（空状態を表示）・`⌘K` コマンドパレット（表示のみ操作不可）・テーブル定義ビュー（開かない） |
| 対象外 | Ask AI / SSH トンネル / エラーの候補提示「近い名前・修正して再実行」                                      |
| 順序   | Oracle を先に全機能。PostgreSQL は `tokio-postgres` で Driver trait の別実装として後から追加              |

エラー表示はメッセージタブのテキストのみとする。デザイン状態 5b の波線・行番号の赤ドット・候補提示・「修正して再実行」ボタンは実装しない。

### SQL の実行単位

`⌘⏎` はカーソル位置の文、`⇧⌘⏎` は選択範囲を実行する。文の切り出しは、文字列リテラル・コメント・Oracle の `q'[...]'` 引用符リテラル・`BEGIN…END;` ブロックを考慮した自前パーサで行う。デザインのメニュー 2 項目がそのままこの意味になる。

### 値の受け渡し

各セルは `{ text: string, kind: "number"|"text"|"datetime"|"bool"|"binary"|"null" }` として送る。

- Oracle の `NUMBER` は最大 38 桁の 10 進数であり、`f64` に入れると精度が壊れる。**文字列のまま保持する。**
- `kind` は結果テーブルの右寄せ判定に使う。
- `CLOB` は先頭 64KB まで、`BLOB` / `RAW` は `[BLOB 1.2 KB]` と表示する。
- `NULL` は `--fg5` の淡色・斜体で `NULL` と表示し、空文字列と区別する（デザインに指定が無いための追加）。

### バインド変数

業務で貼り付ける SQL の多くは `:id` のようなバインド変数を含む。実行の直前にダイアログで値を尋ね、SQL と一緒に Rust へ渡す。

- **抽出はフロントエンドで行う。** `src/sql/statements.ts` の `collectBindVariables` が、文の切り出しと同じ走査を使い回して名前を集める。文字列リテラル・コメント・`q'[...]'` 引用符リテラルの中の `:` は拾わず、`::` や `:=` のような記号も変数と見なさない。同じ名前（大文字小文字の違いは同一）は 1 つにまとめ、出てきた順に並べる。
- **値はすべて文字列として渡し、Oracle 側では `VARCHAR2` としてバインドする。** 型を選ばせる UI は作らない。日付や数値の列と比べるときは暗黙変換が起き、索引が効かなくなる場合があることは承知のうえで、まずは動くことを取る。型指定が要るようになったら、そのときにダイアログへ型の欄を足す。
- **NULL はダイアログのチェックで表す。** チェックの付いた行は Rust 側で `Option<String>` の `None` としてバインドする。空文字列との区別は Oracle には無いが、意図として区別できる形にしておく。
- **`Driver::execute` / `explain_plan` / `actual_plan` が `binds: &[Bind]` を取る。** `Bind` は `(String, Option<String>)` であり、`serde` では 2 要素の配列になる。計画を取るときもバインドが要る（`EXPLAIN PLAN FOR` でも値を与えないと `ORA-01008` になる）。`ExecuteOutcome` は変えていない。
- **文に無い名前は捨てる。** 名前の抽出は自前パーサであり、Oracle の見立てとずれうる。余分な名前をそのまま渡すと `ORA-01036` で実行そのものが落ちるため、`Statement::bind_names()` に載っている名前だけを束ねる（`src-tauri/src/db/oracle/bind.rs`）。足りない場合は Oracle が `ORA-01008` を返し、その旨が利用者に届く。
- **前回の値はタブごとに覚える。** 置き場所は `execution` ストアではなく `tab` ストアで、変数名をキーにした表として持つ。セッション（[0005](0005-クエリ履歴とセッション復元のsqlite.md)）には保存しないため、再起動すれば消える。
- **履歴には値を記録しない。** 記録するのは SQL 本体だけである。値には個人情報が入りうるうえ、履歴は SQLite に平文で残るためである（[0005](0005-クエリ履歴とセッション復元のsqlite.md)）。同じ理由でセッションにも書かない。

ダイアログは `src/components/editor/BindValuesDialog.tsx`。見た目と作りは CSV 保存ダイアログに揃え、`⏎` で実行、`esc` で取り消す。キーバインドは足していない（`⌘⏎` / `⇧⌘⏎` / `⌘E` / `⇧⌘E` の途中に挟まる）。

### 実行計画

- `⌘E` = `EXPLAIN PLAN FOR <sql>` → `DBMS_XPLAN.DISPLAY`（実行しない見積り）
- `⇧⌘E` = SQL に `/*+ GATHER_PLAN_STATISTICS */` を注入して実際に実行 → `DBMS_XPLAN.DISPLAY_CURSOR(format => 'ALLSTATS LAST')`（E-Rows と A-Rows の乖離が見える）

`⇧⌘E` は SQL を書き換えて実行するため、`SELECT` 以外に対しては事前に確認ダイアログを出す。計画の表示はテキストのままで、ツリー UI へのパースは行わない（`DBMS_XPLAN` の出力は既に整形済みである）。

### メッセージタブ

実行ログを時系列で追記する。実行時刻・SQL 全文・所要時間・影響行数に加え、データベースからの通知（Oracle は `DBMS_OUTPUT.GET_LINES`、PostgreSQL は `NOTICE`）を出す。`DBMS_OUTPUT` が無いと無名 PL/SQL ブロックを実行しても何も見えない。

### エディタとタブ

- CodeMirror 6 + `@codemirror/lang-sql`。接続種別で方言を切り替え、取得済みスキーマ（[0007](0007-スキーマ取得の段階的ロード.md)）を渡してテーブル名・列名の補完を効かせる。
- タブは `.sql` ファイルの開く/保存に対応する。未保存バッファも SQLite に保存し、再起動でタブ構成ごと復元する。新規タブ名は `無題-1.sql`。
- 検索・置換は `@codemirror/search` の標準パネルを使う。自前で組まないのは、正規表現・単語単位・大小の区別・置換の反復といった細部を作り直す値打ちが無いためである。素のままだと文言が英語で意匠も浮くので、`EditorState.phrases` で日本語へ差し替え、見た目は `koduchiEditorTheme` がトークン経由で与え、閉じるボタンだけ lucide のアイコンへ挿げ替える（`src/components/editor/search.tsx`）。検索欄は `keyup` のたびに問い合わせを作り直す作りなので、日本語入力の変換中の打鍵だけは捨て、未確定の文字列で検索が走らないようにしてある。

### 結果テーブル

TanStack Virtual による仮想スクロール。スクロールが下端に近づくたびに 1,000 行ずつ追加取得する（[0003](0003-結果セットのカーソル保持と接続プール.md)）。

#### 列幅

列幅は次の 3 通りで決まる。

- 何もしなければ型ごとの既定値（数値は狭く、文字列は余った幅を分け合って伸びる）
- 列見出しの右端をドラッグして手で決める（当たり判定 6px、カーソルは `col-resize`）
- 列見出しをダブルクリックして内容に合わせる（上限 600px、下限 48px）

内容に合わせるときは実寸を測らない。数十万行ぶんのセルを測ると描画が止まるためである。同梱の PlemolJP は半角と全角の送りが 1:2 に揃った等幅書体なので（`CLAUDE.md` の「書体」節）、全角を 2 文字ぶんとして数えた文字数に半角 1 文字の送り幅を掛ければ十分な精度で見積もれる。走査するのは取得済みの行だけであり、続きを読み込んだあとは合わせ直すことになる。

決めた幅は**列名をキーにしてタブごとに**覚える。同じクエリを実行し直しても幅が保たれる。設定ファイルやセッション（[0005](0005-クエリ履歴とセッション復元のsqlite.md)）へは保存せず、再起動すれば既定へ戻る。タブを閉じたときに忘れる。

#### セルの詳細

セルをダブルクリックすると結果ペインの右側に開く。テーブルの行は 1 行の高さに収まる範囲しか見せられず、`CLOB` は先頭 64KB まで持っているのに末尾が切れてしまうためである。等幅で全文を出し、折り返しの有無を切り替えられる。先頭が `{` か `[` で `JSON.parse` に通る値は `JSON.stringify(value, null, 2)` で整形して出し、整形するかどうかは切り替えられる。値の文字数と列の型名を添える。`BLOB` / `RAW` はバイト列がフロントエンドに無いため（「値の受け渡し」節）、その旨を書く。`esc` で閉じる。

#### 並べ替えを実装しない理由

**列のソートは実装しない。** 結果は 1,000 行ずつの分割取得である（[0003](0003-結果セットのカーソル保持と接続プール.md)）。取得済みの行だけを並べ替えると、全体を並べ替えたように見えて実際には手元の一部しか並んでいない。読み手はそれを見分けられず、上位 10 件を眺めて誤った結論を出す。かといって並べ替えのたびに全件を取り直せば、分割取得にした意味が消える。並べ替えたいときは `ORDER BY` を書くのが筋である。同じ理由で列の並べ替えと非表示も実装しない。

#### セル選択とコピー

「1 セルを拾って別のクエリの `WHERE` に貼る」は DB クライアントで最も多い操作であり、ファイル経由の CSV 保存では代われない。ブラウザの文字選択は CSS grid のレイアウトでは桁が崩れるため使わず、セル単位の矩形選択を自前で持つ。

- 選択: クリックで 1 セル、`⇧` + クリックまたはドラッグで矩形、行番号の列をクリックで行全体、`⌘A` で表示中の全行。選択中のセルは `bg-fill` で塗り、範囲の外枠を `--ac` で引く。
- キーボード: 矢印キーで移動、`⇧` + 矢印で伸長、`Home` / `End` で行頭・行末。仮想スクロールのため、選択が画面外へ出たら `virtualizer.scrollToIndex` で追う。
- コピー: `⌘C` は区切りがタブ、行区切りが `\n`。値は画面と同じ規則で NULL は `NULL`。`⇧⌘C` は列見出しを 1 行目に付ける。右クリックのメニューは「コピー」「見出し付きでコピー」「この列をコピー」の 3 つ。
- 選択の状態は `ui` ストアではなく `ResultTable` の中に閉じる。打鍵ごとにアプリ全体が描き直らないようにするためである。タブを切り替えたときは `ResultPane` が `key` を変えて作り直し、選択はその場で捨てる。
- クリップボードは `@tauri-apps/plugin-clipboard-manager`。呼び出しは `src/api/clipboard.ts` に包み、テストから差し替える（[0010](0010-テストとリンタの構成.md)）。`capabilities/default.json` に `clipboard-manager:allow-write-text` が要る。
- セルの編集・貼り付け・CSV 書式でのコピーは対象外。

#### マウス操作の割り振り

選択・詳細・列幅がひとつの表の上に同居するため、場所と回数で分けてある。

| 操作                   | 起きること                       |
| ---------------------- | -------------------------------- |
| セルを単クリック       | そのセルを選ぶ（ドラッグで矩形） |
| セルをダブルクリック   | 詳細パネルを開く                 |
| セルを右クリック       | コピーのメニュー                 |
| 行番号を単クリック     | 行全体を選ぶ                     |
| 見出しの右端をドラッグ | 列幅を変える                     |
| 見出しをダブルクリック | 列幅を内容に合わせる             |

ダブルクリックは 1 回目の押し下げで選択も起こるため、詳細を開いたセルはそのまま選択されている。列幅のつまみは見出しの中にあり、本文のセル選択とは押し下げを拾う場所が違う。

### CSV の書式

保存ダイアログで次を選ばせ、選択内容は次回のために保存する。

- 区切り文字: **カンマ**（既定） / タブ / セミコロン
- 文字コード: **UTF-8 (BOM 付き)**（既定、Excel 対策） / UTF-8 (BOM なし) / Shift_JIS
- NULL の表現: **空欄**（既定） / `NULL` / `\N`

固定: ヘッダー行あり、`"` 囲みの RFC 4180 エスケープ、改行は CRLF。

### キーバインド

| キー        | 動作                             |
| ----------- | -------------------------------- |
| `⌘⏎`        | 実行（カーソル位置の文）         |
| `⇧⌘⏎`       | 選択範囲のみ実行                 |
| `⌘E`        | 実行計画を生成                   |
| `⇧⌘E`       | 実測付きで生成                   |
| `⌥⌘S`       | 結果を CSV で保存                |
| `⌘.`        | 実行を中止                       |
| `⌘F`        | エディタ内を検索                 |
| `⌘G` / `⇧⌘G` | 次の一致 / 前の一致             |
| `⌥⌘F`       | エディタ内を置換                 |
| `⌘S` / `⌘O` | 保存 / 開く                      |
| `⌘T` / `⌘W` | 新しいタブ / タブを閉じる        |
| `⌃⌘N`       | 別の接続を新しいウィンドウで開く |
| `⌘C` / `⇧⌘C` | 選択したセルをコピー / 見出し付きでコピー |
| `⌘A`        | 結果テーブルの表示中の全行を選択 |

`⌘C` / `⇧⌘C` / `⌘A` は結果テーブルに焦点があるときだけ効く。表の中でしか意味を持たないため、`App.tsx` ではなく `ResultTable` の `keydown` に置く。

`⌘K`（コマンドパレット）と `⌘I`（Ask AI）は**割り当てない**。将来のために予約する。

### 接続の切断と切り替え

ステータスバーの「接続中」を押すとメニューが開き、「切断」と「別の接続へ切り替え…」の 2 項目を出す。どちらも同じ動きで、接続を閉じて接続を選ぶ画面へ戻す。文言を 2 つ置いてあるのは、切り替えのつもりの利用者に「切断」しか見えないと、その道が無いように見えるためである。1 接続 = 1 ウィンドウ（[0009](0009-ウィンドウモデルとタイトルバー.md)）であるため、接続を操作する入口は「今どこへ繋がっているか」を出している所に置く。

- **エディタのタブと内容は残す。** 切断でタブを失うと、書きかけの SQL のために接続を切れなくなる。
- **切断の前に開いている結果セットを `release_tab` で閉じる**（[0003](0003-結果セットのカーソル保持と接続プール.md)）。開いたままのカーソルはデータベース側の資源を握る。そのうえで `execution` ストアと `schema` ストアの状態を捨てる。
- **後片付けは `App.tsx` が順に呼ぶ。** `connection` ストアの `disconnect` に含めない。ストア同士を結合させないためである。
- **実行中の文があるときは切断しない。** 走っている文を接続ごと打ち切ると、何が起きたのか分からないまま結果を失う。ダイアログで `⌘.` による中止を促す（そのダイアログからも中止を掛けられる）。キーバインドは増やしていない。

### 設定画面（ステータスバー右の ⚙︎）

テーマ 3 択 / Oracle Instant Client のパス（未検出時のみ表示）/ 履歴の一括削除 / 罫線の有無（`--gl`）/ 行の高さ（`--rp`）。

### ペインの大きさ

サイドバーと本体の境界、エディタと結果ペインの境界をドラッグして大きさを変えられる。区切りは `src/components/layout/Splitter.tsx` の 1 部品で、縦横どちらにも使う。当たり判定は 6px、見た目の線は 1px（`--line`、ホバーとフォーカスで `--ac`）。親の `gap-6px` の中へ負の余白で重ねて置くため、区切りを足しても隣り合うペインの間隔は変わらない。

- **下限と上限**: サイドバーは 180〜480px、エディタの高さは 120px 〜（ウィンドウの高さ − 200px）。ウィンドウを縮めたときに下限を割らないよう、`resize` のたびに丸め直す。値と丸めは `src/components/layout/paneSizes.ts` に集めてある。
- **既定値**: サイドバー 240px、エディタ 268px。境界のダブルクリックで戻る。
- **キーボード**: `role="separator"` に焦点を当てて矢印キーで 8px、`⇧` + 矢印で 32px 動く。エディタの中でしか効かない `⌘⏎` などと同じく、焦点のある場所でしか意味を持たないため、`App.tsx` のキーバインド表には載せない。
- **入力**: `pointerdown` / `pointermove` / `pointerup` と `setPointerCapture` で扱う。`mousemove` を `window` に貼るより外れにくい。ドラッグ中は `document.body` に `user-select: none` を当てる。
- **保存先**: 値はウィンドウごとのセッション（[0005](0005-クエリ履歴とセッション復元のsqlite.md) の `history.sqlite3`）に置く。`settings.toml` ではない。ウィンドウごとに違ってよい値だからである。`SessionState` の `sidebarWidth` / `editorHeight` はどちらも省略でき、この 2 つを持たない古いセッションは既定値へ落ちる（`session_state` の列は起動時に無ければ足す）。

ペインの分割・入れ替え・タブの引き剥がしは行わない。境界を動かすだけである。

### 状態管理

zustand を機能別に 6 ストアへ分割する（`connection` / `tab` / `execution` / `history` / `schema` / `ui`）。ストアは Tauri の `invoke` を直接呼ばず、`src/api/` 層越しに呼ぶ。これによりストアの単体テストをデータベースから切り離せる（[0010](0010-テストとリンタの構成.md)）。

### ディレクトリ構成

```
src/
  api/            Tauri invoke のラッパ（テスト時に差し替える境界）
  stores/         zustand ストア6種
  components/
    titlebar/  sidebar/  editor/  results/  connection/  settings/  layout/
  theme/          CSSトークン定義（:root / [data-theme="dark"]）
  types/          Rust と共有する型定義
src-tauri/src/
  lib.rs          commands 登録のみ
  commands/       Tauri コマンド（薄い層）
  db/
    driver.rs     Driver trait
    oracle/       接続・実行・スキーマ取得・実行計画・DBMS_OUTPUT
    actor.rs      接続ごとのスレッドとチャネル
  tnsnames/       tnsnames.ora パーサ
  config/         connections.toml の読み書き
  keychain/       keyring ラッパ
  history/        SQLite（履歴・タブ復元）
  csv/            CSV 書き出し
```

## デザインからの意図的な変更

出典は Claude Design プロジェクト <https://claude.ai/design/p/11b6f2ab-681b-437c-a5f0-1ddf7e59fbbe?file=DB+Client.dc.html> の `DB Client.dc.html`（3a が本体レイアウト、5a〜5g が状態パターン）。同梱の `support.js` はキャンバスの実行時ランタイムであり、アプリ実装とは無関係。

1. **実行計画タブを追加** — デザインにメニュー項目（`⌘E` / `⇧⌘E`）はあるが表示先が無かった。
2. **NULL の表示を定義** — デザインに指定が無かった。
3. **接続フォームに方式セグメントを追加** — TNS 対応のため（[0006](0006-tnsnames解決を自前パースで行う.md)）。
4. **結果ペインのタブを動的に** — 固定 3 タブ（結果 / メッセージ / 履歴）から、履歴をサイドバーへ移し、必要時のみ出現する方式へ（[0005](0005-クエリ履歴とセッション復元のsqlite.md)、[0009](0009-ウィンドウモデルとタイトルバー.md)）。
5. **接続を選ぶ画面を手前に置いた** — デザイン 5g は「接続を作成」1 画面で、保存済みの接続はその中の一覧だった。日常の起動は保存済みへ繋ぐことが大半であるため、選ぶ画面を手前に立て、そこから新規作成と編集へ進む形にした。選んだ行はその場で繋ぐ。パスワードがキーチェーンに無ければ行の中で尋ね、TNS は繋ぐ直前に tnsnames.ora を読み直してエイリアスを解決する（[0004](0004-認証情報と接続設定の保存先.md)、[0006](0006-tnsnames解決を自前パースで行う.md)）。
6. **接続を作成する画面にテスト接続を置いた** — 保存や接続をせずに繋がるかだけを確かめる `test_connection` コマンドを足した。1 本だけ繋ぎ、サーバのバージョンを読んですぐ閉じる。本番と同じ手順（`DBMS_OUTPUT` の有効化、読み取り専用トランザクション）を通るため、繋いでからでないと分からない失敗もここで拾える。結果には試した接続情報の写しを添え、入力が変わった時点で古い結果を隠す。
